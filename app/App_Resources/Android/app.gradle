// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "pl.mgontar.meteoApp"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
